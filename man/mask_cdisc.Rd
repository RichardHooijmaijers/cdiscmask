% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask.R
\name{mask_cdisc}
\alias{mask_cdisc}
\title{Mask multiple CDISC domains consistently}
\usage{
mask_cdisc(domains, key = NULL)
}
\arguments{
\item{domains}{A named list of data frames, one per CDISC domain
(e.g. \code{list(DM = dm_df, AE = ae_df, LB = lb_df)}).}

\item{key}{A \code{cdiscmask_key}. If \code{NULL} a new key is created automatically.}
}
\value{
A list with the same names as \code{domains}, each element being the
masked data frame. The key used is attached as attribute \code{"key"}.
}
\description{
Convenience wrapper that calls \code{\link[=mask_domain]{mask_domain()}} on each element of \code{domains}
using a shared \code{key}, guaranteeing cross-domain consistency of subject-ID
pseudonyms and date shifts.
}
\examples{
if (requireNamespace("safetyData", quietly = TRUE)) {
  masked <- mask_cdisc(list(
    DM = safetyData::sdtm_dm,
    AE = safetyData::sdtm_ae
  ))
  attr(masked, "key")
}
}
