% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask.R
\name{mask_domain}
\alias{mask_domain}
\title{Mask a single CDISC domain data frame}
\usage{
mask_domain(df, key, domain = NULL, cols = NULL)
}
\arguments{
\item{df}{A data frame for one CDISC domain.}

\item{key}{A \code{cdiscmask_key} created by \code{\link[=new_mask_key]{new_mask_key()}}.}

\item{domain}{Optional domain name passed to \code{\link[=detect_sensitive_cols]{detect_sensitive_cols()}}.}

\item{cols}{An optional named list overriding automatic detection; same
structure as the return value of \code{\link[=detect_sensitive_cols]{detect_sensitive_cols()}}.}
}
\value{
A data frame with sensitive values replaced. The attribute
\code{"cdiscmask_cols"} records which columns were masked.
}
\description{
Applies anonymization while preserving column types and recording every
transformation in \code{key} so that \code{\link[=unmask_domain]{unmask_domain()}} can reverse it. When the
same \code{key} is reused across multiple domains, subject-ID pseudonyms and date
shifts are guaranteed to be consistent.
}
\details{
The masking order is:
\enumerate{
\item Date columns are shifted (keyed by the original subject ID).
\item Subject-ID columns are replaced with pseudonyms.
\item Free-text columns are redacted.
}

The original column classification is stored as the \code{"cdiscmask_cols"}
attribute on the returned data frame so that \code{\link[=unmask_domain]{unmask_domain()}} knows what to
restore.
}
\examples{
if (requireNamespace("safetyData", quietly = TRUE)) {
  key       <- new_mask_key()
  dm_masked <- mask_domain(safetyData::sdtm_dm, key, domain = "DM")
  dm_masked[1:3, c("USUBJID", "RFSTDTC", "AGE", "SEX")]
}
}
