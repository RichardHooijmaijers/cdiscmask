% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subject_id.R
\name{mask_subject_id}
\alias{mask_subject_id}
\title{Replace subject identifiers with pseudonyms}
\usage{
mask_subject_id(df, col = "USUBJID", key, prefix = "SUBJ-")
}
\arguments{
\item{df}{A data frame.}

\item{col}{Name of the subject-ID column (e.g. \code{"USUBJID"}).}

\item{key}{A \code{cdiscmask_key} created by \code{\link[=new_mask_key]{new_mask_key()}}.}

\item{prefix}{String prepended to the zero-padded integer pseudonym
(default \code{"SUBJ-"}).}
}
\value{
\code{df} with \code{col} replaced by pseudonyms. \code{key} is updated in place.
}
\description{
Maps every unique value in \code{col} to a new opaque identifier and stores the
mapping in \code{key}. If a subject was already mapped by a previous call (e.g.
from another domain), the existing pseudonym is reused.
}
