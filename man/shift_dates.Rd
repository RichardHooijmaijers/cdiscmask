% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dates.R
\name{shift_dates}
\alias{shift_dates}
\title{Shift date/datetime columns by a per-subject random offset}
\usage{
shift_dates(df, cols, subject_col = "USUBJID", key, range = c(-365L, 365L))
}
\arguments{
\item{df}{A data frame.}

\item{cols}{Character vector of column names to shift.}

\item{subject_col}{Name of the subject-ID column used to look up or create
per-subject offsets in \code{key} (default \code{"USUBJID"}).}

\item{key}{A \code{cdiscmask_key} created by \code{\link[=new_mask_key]{new_mask_key()}}.}

\item{range}{Integer vector of length 2 giving the inclusive range (days)
from which offsets are sampled (default \code{c(-365L, 365L)}).}
}
\value{
\code{df} with \code{cols} shifted. \code{key} is updated in place.
}
\description{
Each subject receives a random integer offset (in days) drawn once and
stored in \code{key}. The same offset is reused across domains, preserving
within-subject temporal relationships.
}
\details{
Three column types are handled:
\itemize{
\item \strong{Character DTC/DTM} (SDTM): ISO-8601 strings. Full dates (\code{YYYY-MM-DD},
optionally followed by a time component) are shifted; partial dates
(\code{YYYY-MM}, \code{YYYY}) and the CDISC \code{"--"} placeholder are left untouched.
\item \strong{\code{Date} objects} (ADaM): shifted by adding the offset.
\item \strong{Integer/numeric DY} (study days): shifted arithmetically without date
parsing.
}
}
\examples{
key <- new_mask_key()
df  <- data.frame(
  USUBJID = c("01-001", "01-001", "01-002"),
  RFSTDTC = c("2020-01-15", "2020-06-01", "2019-11-20"),
  stringsAsFactors = FALSE
)
shift_dates(df, cols = "RFSTDTC", key = key)
}
